# DESIGN.md

## 视觉意图

Semattice 管理中心使用“浅色目录账页”方向：暖白画布、轻微冷灰导航面、深墨蓝文字和克制钴蓝强调。管理员通常在明亮办公室里长时间审阅配置，因此选择浅色、高密度、低疲劳的产品界面，而非现有公开站点的深色技术叙事。

## 版式

- 桌面应用壳：固定左导航、低高度顶栏、可滚动的主工作区。
- 对象与字段：对象目录、字段矩阵、对象检查器三栏并列；目录与检查器保持稳定，字段表承担主要阅读任务。
- 其他治理页面：标题与摘要带后接主体表格、时间线或树形结构。避免无差别卡片网格和嵌套卡片。
- 页面在 1280px 及以上宽度优先优化；本次不增加移动端专属布局。

## 色彩与文字

- 画布：偏暖的近白；层次由淡蓝灰面和 1px 分隔线建立。
- 文字：深蓝灰为主，次级文字降低对比度但保持 AA。
- 强调：钴蓝只用于当前导航、关键链接、焦点和少量筛选状态；健康状态使用低饱和青绿，风险状态使用克制的橙红。
- 字体：中文系统无衬线字体；对象标识、字段标识和版本号使用等宽字体。
- 默认正文 13px，辅助文字 11–12px，表头 11px；避免为了层级使用过大的字号。

## 组件语汇

- 导航选中项采用左侧 2px 蓝线和极浅背景，不位移、不发光。
- 表格为核心组件：固定清晰的表头、行分隔线、悬停底色和可见的键盘焦点；不将 table 元素改成 div/flex。
- 状态采用文字加低饱和圆点或细标签，颜色不作为唯一含义。
- 控制台只读，不出现创建、编辑、删除或保存按钮；搜索、筛选、分页、查看明细和展开/收起是允许的交互。
- 动效只用于页面切换、加载骨架和导航焦点，时长 150–200ms，遵循 reduced-motion。

## 页面状态

每个页面至少包含默认、加载、空、错误、无权和长文本截断状态。无权页面说明缺少管理权限，并提供回到 AgentCiCi 的安全入口；不得泄漏对象名、字段名或审计内容。
